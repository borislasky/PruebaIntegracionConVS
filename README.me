# Librería de conexión WiFi síncrona

Se pueden definir más de un SSID.

Es necesario un fichero auth.h con el siguiente contenido:

```cpp

#ifndef AUTH_WIFI_h
#define AUTH_WIFI_h

#define MAX_SSIDs 	10 	// Máximo número de SSIDs
#define MAX_L_SSID 	25	// Longitud máxima de los SSIDs
#define NUM_SSIDs 	 3	// Número de SSIDs

char ssids[NUM_SSIDs][MAX_L_SSID] = {"SSID1", "SSID2", "SSID3"};
char passs[NUM_SSIDs][MAX_L_SSID] = {"pass1", "pass2", "pass3"};

#endif
```
